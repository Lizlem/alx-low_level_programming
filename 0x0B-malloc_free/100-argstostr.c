#include "main.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>

/**
 * argstostr - concatenates arguments
 * @ac: integer
 * @av: string
 *
 * Return: 0
 */

char *argstostr(int ac, char **av)
{
	if (ac == 0 || av == NULL)
		return (NULL);
	return (0);
}
