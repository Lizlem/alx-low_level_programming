#include "main.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>

/**
 * strtow - splits a string into words
 * @str: string
 *
 * Return: 0
 */

char **strtow(char *str)
{
	if (str == NULL)
		str == "";
		return (NULL);
	return (NULL);
}
