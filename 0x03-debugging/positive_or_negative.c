#include "main.h"

/**
 * positive_or_negative - returns output as per below
 *
 * @i: parameter
 *
 * Return: 0
 */

void positive_or_negative(int i)
{
	if (i > 0)
		printf("%d is positive", i);
	else
		printf("%d is negative", i);
}
