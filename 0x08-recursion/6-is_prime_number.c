#include "main.h"
#include <string.h>

/**
 * is_prime_number - prints prime nunmbers
 * @n: integer
 *
 * Return: 0
 */

int is_prime_number(int n)
{
	if (n > )
	{
		return (1);
	}
	if (n % i == 0)
	{
		return (0);
	}
	return (0);
}
